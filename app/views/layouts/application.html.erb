<!DOCTYPE html>
<html>
  <head>
    <title>Kkb</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
    <%= yield %>

    <footer>
      <% if user_signed_in? && !@is_menu %>
        <%= current_user.name_with_code %> |
        <%= link_to "ログアウト", destroy_user_session_path, method: :delete, :class => 'navbar-link', :target => '_top'  %>
      <% end %>
    </footer>
  </body>
</html>
